{
  "name": "ar-pinata",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npm run build && python3 -m http.server 5174 --directory dist",
    "test": "node --check scripts/build.js && node --check src/main.js && node --check src/baby-octopus.js"
  }
}
